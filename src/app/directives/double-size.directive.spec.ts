import { DoubleSizeDirective } from './double-size.directive';

describe('DoubleSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleSizeDirective();
    expect(directive).toBeTruthy();
  });
});
